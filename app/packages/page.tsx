import PackagesGrid from "@/components/Packages/packages-grid";

const page = async () => {
  return (
      <main className="container py-10">
       <PackagesGrid />
      </main>
    );
};
export default page;
